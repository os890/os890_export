Requirements:
 - ExtVal 1.x.3-SNAPSHOT+

It allows to force a validation priority (independent of the used order).
It's especially useful for the metadata-provider add-on. So you can prioritize the found constraints.